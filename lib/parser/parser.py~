class cls_xml_parser:
	
	def __init__(self):

		import xml.etree.ElementTree as ET
		

	def fun_xml_tree_check(self,str_input_xml_file):

		tree_xml_parse = ET.parse(str_input_xml_file)
		obj_root = tree.getroot()
		
		# Check if we have LogicArguments, Topology, and ModelChecking

		lst_root_children = []
		for obj_child in obj_root:
			lst_root_children.append(obj_child.tag)

		if 'Topology' in lst_root_children:
			## check for sensible settings
		else:
			## use default settings

		if 'LogicArguments' in lst_root_children == True and 'ModelChecking' in lst_root_children == True:
			## check each of them for sensible inputs

		else:
			## exit to error routine


		












	
